<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Details</title>
    <link href="/output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100 font-sans">
    <div class="container mx-auto p-6">
      <h1 class="text-3xl font-bold mb-4">File Details</h1>
      <div class="bg-white p-6 rounded shadow">
        <p class="text-lg font-medium mb-2">Name: <%= file.name %></p>
        <p class="text-lg font-medium mb-2">Size: <%= file.size %> bytes</p>
        <p class="text-lg font-medium mb-2">
          Upload Time: <%= file.createdAt %>
        </p>
        <a
          href="/files/download/<%= file.id %>"
          class="text-blue-500 hover:underline"
          >Download</a
        >
      </div>
    </div>
  </body>
</html>
